<%= form_with(model: king) do |form| %>
  <% if king.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(king.errors.count, "error") %> prohibited this king from being saved:</h2>

      <ul>
        <% king.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
